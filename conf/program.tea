Program(x:0 y:1 z:0 fib:30) {

	({fib} -= 1 >= 0).While {
		{z}.Set(to:{x} + {y});
		{x}.Set(to:{y}).Print;
		{y}.Set(to:{z});
	}
	
	
	If(this.parent.x == 40) {
	
	}
	
	Set(z to:30)
}

Program(x:0 y:1 z:0 fib:30) {
	While(fib -= 1 >= 0) {
		Set(z to:x + y)
		Set(x to:y)
		Set(y to:z)
	}
	
	Print(x)
}